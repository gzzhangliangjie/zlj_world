# ---- staging & transient (never upload) ----
_external/
_logs/
Vselftest_*.log
Vtmp_*.png
Vpatch_*.ps1

# ---- local-only builds (the shipped artifact is WebGL) ----
VoxelCraft/Builds/Windows/

# ---- OS ----
Thumbs.db
.DS_Store

# ---- Mojang 版权材料:仅本地参考,禁止再分发(EULA) ----
_vanilla-src/
_refs/vanilla/

# Unity batch logs (scratch)
unity-*.log
